#include <bits/stdc++.h>
using namespace std;
vector<int> findDuplicates(vector<int>& n) {
    for(int i=0;i<n.size();i++){
        if(n[i] > 0){
            
        }
    }
}
int main(){
    vector<int> x = {4,3,2,7,8,2,3,1};
    vector<int> y = findDuplicates(x);
    cout<<endl;
return 0;
}