#include <bits/stdc++.h>
using namespace std;
int subarraysDivByK(vector<int>& n, int k) {
    int ans = 0;
    for(int i=0;i<n.size();i++){
                        
    }
}
int main(){
    vector<int> x;
    int k;
    for(int i=0;i<k;i++){
        int y;cin>>y;
        x.push_back(y);
    }
    int c; cin>>c;
    int ans = subarraysDivByK(x,c);
return 0;
}